# Home — page overrides

Inherits MASTER. Deviations:

- **Hero:** Asymmetric layout — copy left, signature SVG right (stacks on mobile)
- **Signature:** Leak → seal SVG is homepage-only
- **RCJ panel:** Last content section before footer; primary outbound CTA to Rhinos Can’t Jump
- **Section order:** Framing → hubs stages → week one → anti-patterns → documented/never built → RCJ
- **Density:** Keep existing sections; restyle only — do not delete honesty or hub stages
